{
  "name": "ember",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run && wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute ember-journal --remote --file=./schema.sql",
    "test:ui": "node test/ui/checks.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "playwright-core": "^1.63.0",
    "vitest": "~3.0.0",
    "wrangler": "^4.0.0"
  }
}
